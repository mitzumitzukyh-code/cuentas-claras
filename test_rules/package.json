{
  "name": "cuenta-clara-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Pruebas de las reglas de seguridad de Firestore contra el emulador.",
  "scripts": {
    "test": "npx firebase emulators:exec --config ../firebase.json --only firestore --project cuenta-clara-test \"mocha firestore.test.js --timeout 20000\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^13.35.1",
    "mocha": "^10.7.3"
  }
}
